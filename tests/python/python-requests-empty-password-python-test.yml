id: python-requests-empty-password-python
valid:
  - |
    encoded = jwt.encode({"some": "payload"}, secret_key, algorithm="HS256")
    secret_const = 3
invalid:
  - |
    from requests.auth import HTTPProxyAuth
    asd = ''
    basic = HTTPProxyAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    from requests.auth import HTTPDigestAuth
    asd = ''
    basic = HTTPDigestAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    from requests.auth import HTTPBasicAuth
    asd = ''
    basic = HTTPBasicAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    asd = ''
    basic = requests.auth.HTTPProxyAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    asd = ''
    basic = requests.auth.HTTPDigestAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    asd = ''
    basic = requests.auth.HTTPBasicAuth('user', asd)
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    from requests.auth import HTTPProxyAuth
    basic = HTTPProxyAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    from requests.auth import HTTPDigestAuth
    basic = HTTPDigestAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    from requests.auth import HTTPBasicAuth
    basic = HTTPBasicAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    basic = requests.auth.HTTPProxyAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    basic = requests.auth.HTTPDigestAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
  - |
    basic = requests.auth.HTTPBasicAuth('user', '')
    requests.get('https://httpbin.org/basic-auth/user/pass', auth=basic)
