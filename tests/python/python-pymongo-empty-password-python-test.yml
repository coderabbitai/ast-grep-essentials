id: python-pymongo-empty-password-python
valid:
  - |
    MongoClient(password=ugh)
  - |
    MongoClient(password=os.env['secret'])
  - |
    MongoClient(password=os.getenv('secret'))
invalid:
  - |
    from pymongo import MongoClient
    import os
    MongoClient(password="")
  - |
    from pymongo import MongoClient
    import os
    password = "";
    MongoClient(password=password)
