id: python-peewee-pg-hardcoded-secret-python
valid:
  - |
    mysql_db1 = MySQLDatabe('my_app', user='app', password='db_password', host='10.1.0.8', port=3306)
invalid:
  - |
    pg_db1 = PostgresqlDatabase('my_app', user='postgres', password='password', host='10.1.0.9', port=5432)
  - |
    pswd = "password"
    pg_db2 = PostgresqlDatabase('my_app', user='postgres', password=pswd, host='10.1.0.9', port=5432)
  - |
    database = PostgresqlDatabase(None)
    database.init('database_name', host='localhost', user='postgres', password='password')
  - |
    database = PostgresqlDatabase(None)
    pswd = "password"
    database.init('database_name', host='localhost', user='postgres', password=pswd)
  - |
    pswd = "password"
    pg_db2 = PostgresqlExtDatabase('my_app', user='postgres', password=pswd, host='10.1.0.9', port=5432)
  - |
    pool = PooledPostgresqlDatabase('my_app', user='postgres', password='password', host='10.1.0.9', port=5432)
  - |
    db_rch_p = PooledCockroachDatabase('my_app', user='postgres', password='password', host='10.1.0.9', port=5432)
