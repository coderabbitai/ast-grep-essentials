id: python-pymysql-empty-password-python
valid:
  - |
    pymysql.connect(password=CONFIG)
  - |
    pymysql.connect(password=os.env['secret'])
invalid:
  - |
    pymysql.connect(password="")
  - |
    ishan = ""
    PASSWORD1 = CONFIG
    pymysql.connect(password=ishan)
